DISC.pyはバックエンドのメインとなるファイルです。基本的にはこのファイルを実行すればバックエンドはOKです。question.pyはDISC.pyの補助的なファイルでDISC.pyはこれをimportして用いています。
appにはフロントエンドのファイルが入っています。app直下に入っているpage.tsxはメインの質問が並んだページについてのファイルでこれを動かし、指定されたurlに飛べばページが見れます
DISC.pyはuvicorn DISC:app --reloadで実行でき、page.tsxはnpm run devで実行できます。

以下レビューのお願い
①実装した内容について
DISC診断アプリを製作しました。
まずバックエンドで実装したのはフロントエンドで出力した値を取得して質問のタイプごとに点数を計算してタイプをフロントエンド側に出力するプログラムを作りました。
フロントエンドはボタンを押すと点数を取得し、最後に結果を見るボタンを押すと結果のページに飛ばされるようなプログラムを組みました。
②現状困っていること
機能としてこれで十分なのか、セキュリティなどの問題は何も考えていないがよいのか、特に診断ページのUIがあまり良いと言えない
③特に以下の点についてレビューをいただきたいです
・全体の設計・構成が適切か
・可読性（変数名・処理の分け方）
・改善できそうな書き方や設計の方向性
細かい書き方のアドバイスがあればそれもいただきたいです。(特にtypescriptは完全に初学者なので)
初学者のため、的外れな実装になっている可能性がありますが、率直なご意見をいただけると助かります。
